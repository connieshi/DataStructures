CS102 - Data Structures

Given an input text file, compute a list of all unique words and count how many times they occur. Repeat this with a Linked List and Binary Search Tree as the choice of data structure for storing words to compare relative performance.